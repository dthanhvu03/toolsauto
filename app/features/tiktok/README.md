# Feature: tiktok

Adapter TikTok (Playwright) — scaffold / publish path qua dispatcher.

| Entry | Path |
|-------|------|
| Adapter | `adapter.py`, `selectors.py` |

Depends on: `app.adapters.contracts`, `app.adapters.common.*`. Public API: `TiktokAdapter` via `app.features.tiktok`.
