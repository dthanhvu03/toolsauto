# Feature: instagram

Adapter Instagram (Playwright) — scaffold / publish path qua dispatcher.

| Entry | Path |
|-------|------|
| Adapter | `adapter.py`, `selectors.py` |

Depends on: `app.adapters.contracts`, `app.adapters.common.*`. Không có worker PM2 riêng.
