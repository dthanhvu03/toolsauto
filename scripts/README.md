# Scripts

- **`python manage.py`** at the repo root — migrations (`db`), worker (`worker`), viral (`viral`), Facebook pages (`pages scrape`), insights (`insights scrape`), dev server (`serve`).
- **`archive/`** — legacy one-off utilities and old manual migrations; see `archive/README.md`.
